import type { LucideIcon } from "lucide-react";

export interface AuthTestimonial {
  text: string;
  author: string;
}

export interface AuthStatBadge {
  icon: LucideIcon;
  value: string;
  label: string;
}
